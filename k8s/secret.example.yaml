# Template only — do not commit a copy of this file with real values.
#
# Generate the bouncer API key against your CrowdSec instance, e.g.:
#   kubectl exec -n crowdsec deploy/crowdsec -- cscli bouncers add traefik-bouncer
#
# Generate a random forward-auth secret, e.g.:
#   openssl rand -hex 32
apiVersion: v1
kind: Secret
metadata:
  name: crowdsec-bouncer-secret
type: Opaque
stringData:
  api-key: "REPLACE_WITH_CSCLI_BOUNCERS_ADD_OUTPUT"
  forward-auth-secret: "REPLACE_WITH_A_RANDOM_VALUE"
